<%= form_with(model: user_coupone, local: true) do |form| %>
  <% if user_coupone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_coupone.errors.count, "error") %> prohibited this user_coupone from being saved:</h2>

      <ul>
      <% user_coupone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :coupone %>
    <%= form.text_field :coupone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
